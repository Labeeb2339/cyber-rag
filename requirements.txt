chromadb>=0.5
ollama>=0.3
langchain-text-splitters>=0.2
tiktoken
rich
rank-bm25
networkx
pytest
# document ingestion (PDF / Word)
PyMuPDF>=1.24
python-docx>=1.1
# cross-encoder reranker (optional; falls back to the LLM-as-reranker if absent)
sentence-transformers>=2.7
